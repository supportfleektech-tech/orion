-r requirements.txt

# Test + lint toolchain.
pytest>=8,<9
pytest-asyncio>=0.24
ruff>=0.6

# httpx backs FastAPI's TestClient. Without it the entire API suite errors out
# on import, so it belongs here rather than in the optional extras.
httpx>=0.27,<1

# The MCP suite drives a real server subprocess rather than mocking the SDK --
# that is what caught the 2.x renames. Tests skip cleanly without it, but then
# the most valuable coverage in the repo silently does not run.
mcp>=1.10,<3
